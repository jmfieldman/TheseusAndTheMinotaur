#ifndef SETTINGS_SCENE_H
#define SETTINGS_SCENE_H

#include "engine/state_manager.h"

/* Create and return a new SettingsScene state. Caller owns the memory. */
State* settings_scene_create(void);

#endif /* SETTINGS_SCENE_H */
