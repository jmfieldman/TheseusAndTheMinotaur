#ifndef SAVE_SELECT_SCENE_H
#define SAVE_SELECT_SCENE_H

#include "engine/state_manager.h"

/* Create and return a new SaveSelectScene state. Caller owns the memory. */
State* save_select_scene_create(void);

#endif /* SAVE_SELECT_SCENE_H */
