#ifndef TITLE_SCENE_H
#define TITLE_SCENE_H

#include "engine/state_manager.h"

/* Create and return a new TitleScene state. Caller owns the memory. */
State* title_scene_create(void);

#endif /* TITLE_SCENE_H */
